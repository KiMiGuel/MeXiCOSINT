Turn this into an installable Python package

- Restructure into src/mexicosint for proper packaging
- Add pyproject.toml (no more setup.py struggles)
- CLI entry point so users just run 'mexicosint'
- Better module organization (modules/, services/, utils/)
- Updated .gitignore to catch Python build artifacts
- Fresh docs and requirements cleanup
